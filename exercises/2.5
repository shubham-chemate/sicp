(define (exp x a)
  (if (= a 0)
      1
      (* x (exp x (- a 1)))))

(define (cons a b)
  (* (exp 2 a) (exp 3 b)))

(define (car z)
  (define (chk x cnt) 
    (if (= (remainder x 2) 0)
        (chk (/ x 2) (+ cnt 1))
        cnt))
   (chk z 0))

(define (cdr z)
  (define (chk x cnt) 
    (if (= (remainder x 3) 0)
        (chk (/ x 3) (+ cnt 1))
        cnt))
   (chk z 0))